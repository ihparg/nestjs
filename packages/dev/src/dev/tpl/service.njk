import { Injectable } from '@nestjs/common'
import { Resolvable } from '@graphi/nestjs'
{% if imports %}{{imports.replace('[]', '')|safe}}{% endif %}

@Injectable()
@Resolvable('{{servicePath}}')
export class {{serviceName}} {
  {{funcCode|safe}}
}
